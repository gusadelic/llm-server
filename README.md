# LLM Server (llama.cpp + Prebuilt Binaries)

This project runs llama.cpp with prebuilt CUDA binaries stored using Git LFS.

---

## 🚀 Setup (First Time)

### 1. Install Git LFS (IMPORTANT)

```bash
sudo apt-get install git-lfs
git lfs install